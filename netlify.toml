[build]
  command = "bundle exec jekyll build"
  publish = "_site"

[build.environment]
  RUBY_VERSION = "3.1.2"
  JEKYLL_ENV = "production"

[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200

[context.production.environment]
  JEKYLL_ENV = "production"
